apiVersion: v1
kind: ConfigMap
metadata:
  name: gitops-prune-test
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "microservices-stack.labels" . | nindent 4 }}
data:
  purpose: "Temporary GitOps prune test"
